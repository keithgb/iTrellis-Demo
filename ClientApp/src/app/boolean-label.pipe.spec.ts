import { BooleanLabelPipe } from './boolean-label.pipe';

describe('BooleanLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
